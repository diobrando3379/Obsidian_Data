**有效全局 batch（B_eff）= batch_size × NUM_GPUS × update_freq**
 若用线性缩放法则：**lr ∝ B_eff**



例如，原配置 batch_size = 40, update_freq = 1, NUM_GPUS = 1, lr = 5e-4

现在要在一块显存更小的显卡上训练, 当batch_size = 20, update_freq = 2, NUM_GPUS = 1

, lr = 5e-4 -> 这套配置最等效